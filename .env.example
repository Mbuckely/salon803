# Salon 803 Application System - Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# ============================================
# REQUIRED - Google Service Account
# ============================================
# Create a service account in Google Cloud Console with permissions:
# - Google Sheets API (write access to your specific sheet)
# - Google Drive API (write access to your specific folder)
# Copy the entire JSON key file content here (as a single line or properly escaped)
GOOGLE_SA_JSON={"type":"service_account","project_id":"your-project",...}

# ============================================
# REQUIRED - Google Sheets Configuration
# ============================================
# The ID of the Google Sheet where applications will be stored
# Found in the URL: https://docs.google.com/spreadsheets/d/[SHEET_ID]/edit
# The sheet must have a worksheet named "applications" with columns:
# timestamp | name | email | phone | position | availability | experience | file_url | consent | ip_hash | user_agent
GOOGLE_SHEETS_ID=your-google-sheet-id

# ============================================
# REQUIRED - Google Drive Configuration
# ============================================
# The ID of the Google Drive folder where resumes will be stored
# Found in the URL: https://drive.google.com/drive/folders/[FOLDER_ID]
# This folder should be PRIVATE (not publicly shared)
# The service account must have write access to this folder
GOOGLE_DRIVE_FOLDER_ID=your-drive-folder-id

# ============================================
# REQUIRED - hCaptcha Configuration
# ============================================
# Get your keys from: https://dashboard.hcaptcha.com/
# Site key (public, used in frontend)
CAPTCHA_SITE_KEY=your-hcaptcha-site-key

# Secret key (private, used in backend for verification)
CAPTCHA_SECRET=your-hcaptcha-secret-key

# ============================================
# REQUIRED - Application Configuration
# ============================================
# Your production website URL (for CORS allowlist)
# Examples: https://salon803.com or https://www.salon803.com
# In development, you can use http://localhost:5173
APP_BASE_URL=https://yourdomain.com

# A long random string used for IP address hashing
# Generate with: openssl rand -base64 32
# Min length: 32 characters
# NEVER share this value or commit it to version control
PEPPER=long-random-string-for-ip-address-hashing-min-32-chars

# ============================================
# OPTIONAL - Email Notifications
# ============================================
# If you want to receive email notifications for new applications
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_resend_api_key

# Email address where notifications should be sent
INTERNAL_NOTIFY_EMAIL=hr@salon803.com

# ============================================
# Supabase Configuration (Auto-configured)
# ============================================
# These are automatically set by Lovable Cloud
# You should not need to modify these
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# ============================================
# Frontend Configuration
# ============================================
# hCaptcha site key (same as CAPTCHA_SITE_KEY, but for Vite frontend)
VITE_HCAPTCHA_SITE_KEY=your-hcaptcha-site-key

# ============================================
# DEVELOPMENT vs PRODUCTION
# ============================================
# For local development:
# - Use localhost URLs for APP_BASE_URL
# - Use test mode for hCaptcha if needed
# - Consider using a separate test Google Sheet/Drive folder
# 
# For production:
# - ALWAYS use HTTPS URLs
# - Ensure all secrets are properly secured
# - Rotate keys regularly (see SECURITY.md)
# - Never expose .env file or log secret values

# ============================================
# Security Checklist
# ============================================
# ✅ .env is listed in .gitignore
# ✅ PEPPER is at least 32 random characters
# ✅ Google Service Account has minimal permissions
# ✅ Google Drive folder is private (not publicly shared)
# ✅ APP_BASE_URL uses HTTPS in production
# ✅ All secrets are stored securely (not in code)
# ✅ Different credentials for dev vs production
# ✅ Regular key rotation scheduled (see SECURITY.md)
